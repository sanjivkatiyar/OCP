package com.ocp.java.mock.test1;

public class Q23 {

    /**
     * javac --module-source-path src -d out --module foo.bar
     * By using --module option, you are telling the compiler that you want to compile foo.bar module.
     * It will, therefore, look for foo.bar directory under src and compile all the java files
     * stored anywhere under src/foo.bar directory.
     */
}
